// standard emoji list is now presented from remote. this script is no longer used, but kept for reference.
import fs from "node:fs";

const tsContent = `// @ts-nocheck\n// Auto-generated by emoji-gen.ts\nexport const emojis = {\n\n} as const;\n\nexport type EmojiKey = keyof typeof emojis;\n`;

fs.writeFileSync("./lib/generated/emojis.ts", tsContent);
